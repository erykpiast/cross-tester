{
  "name": "cross-tester",
  "version": "1.0.0",
  "description": "Utility for automated cross-browser testing with SauceLabs",
  "main": "dist/index.js",
  "scripts": {
    "lint": "eslint src/",
    "mocha": "mocha --compilers js:babel/register --reporter spec --ui tdd test",
    "test": "npm run lint && npm run mocha",
    "precompile": "rm -rf dist/ && mkdir -p dist",
    "compile": "babel -d dist/ src/",
    "prepublish": "npm run lint && npm run precompile && npm run compile",
    "release": "npm run release-patch",
    "release-patch": "git checkout master && npm run prepublish; git commit -a -m 'Build dist/'; npm version patch && git push origin master --tags && npm publish --access=public",
    "release-minor": "git checkout master && npm run prepublish; git commit -a -m 'Build dist/'; npm version minor && git push origin master --tags && npm publish --access=public",
    "release-major": "git checkout master && npm run prepublish; git commit -a -m 'Build dist/'; npm version major && git push origin master --tags && npm publish --access=public"
  },
  "keywords": [
    "bots"
  ],
  "private": true,
  "author": "Eryk Napierała",
  "license": "ISC",
  "dependencies": {
    "babelify": "^6.1.2",
    "bluebird": "^2.9.30",
    "browserify": "^10.2.4",
    "chalk": "^1.0.0",
    "dedent": "^0.4.0",
    "lodash.assign": "^3.2.0",
    "lodash.defaults": "^3.1.1",
    "lodash.flatten": "^3.0.2",
    "lodash.foreach": "^3.0.3",
    "lodash.mapvalues": "^3.0.1",
    "lodash.omit": "^3.1.0",
    "minifyify": "^7.0.2",
    "minimist": "^1.1.1",
    "moment": "^2.10.3",
    "wd": "^0.3.12"
  },
  "devDependencies": {
    "babel": "^5.6.14",
    "chai": "^3.1.0",
    "eslint": "^1.0.0-rc-1",
    "mocha": "^2.2.5"
  }
}
